/target
models/*
!models/download.sh
*.gguf

# Agent session transcripts, written into the working tree by tooling.
session-*.md

# tests/proptest-regressions/ is deliberately NOT ignored — those seeds are
# committed so a failing case re-runs for everyone, per proptest's guidance.
